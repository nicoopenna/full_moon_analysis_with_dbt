0. Setup venv
1. mkdir %userprofile%\.dbt or mkdir ~/.dbt
2. (.venv) E:\dbt-course>dbt init dbtlearn
    Which database would you like to use?
    [1] snowflake
    Enter a number: 1
    account (https://<this_value>.snowflakecomputing.com): <fill with snowflake>
    user (dev username): <available in git>
    password (dev password): <available in git>
    role (dev role): <available in git>
    warehouse (warehouse name): <available in git>
    database (default database that dbt will build objects in): <available in git>
    schema (default schema that dbt will build objects in): <available in git>
    threads (1 or more) [1]: 1
3. cd dbtlearn
4. dbt debug 